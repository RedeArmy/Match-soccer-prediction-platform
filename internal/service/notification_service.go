package service

// TODO: implement NotificationService.
//
// NotificationService dispatches user-facing notifications in response to
// domain events: match results confirmed, ranking updated, quiniela invite
// received. The delivery channel (email, push notification, in-app) should
// be abstracted behind an interface so that the transport can be swapped
// without modifying business logic.
