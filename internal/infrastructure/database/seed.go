package database

// TODO: implement development data seeding.
//
// The Seed function should insert a deterministic set of fixtures (users,
// matches, predictions) into a freshly migrated database to provide a
// realistic starting state for local development and integration tests.
// It must never be called in production; guard the call site with an
// environment check or a dedicated --seed flag in cmd/migrate.
