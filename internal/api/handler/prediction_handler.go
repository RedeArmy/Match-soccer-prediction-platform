package handler

// TODO: implement PredictionHandler with routes:
//   POST   /api/v1/predictions          — submit a prediction for a match
//   PUT    /api/v1/predictions/{id}     — update a prediction (before kick-off only)
//   GET    /api/v1/predictions          — list the authenticated user's predictions
//   GET    /api/v1/predictions/{id}     — get a single prediction
