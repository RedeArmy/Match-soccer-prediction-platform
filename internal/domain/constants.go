package domain

// TODO: define domain-wide constants that do not belong to a specific entity.
// Examples: scoring rules (points for exact result, points for correct outcome),
// tournament constraints (max predictions per user, deadline offsets before kick-off).
// Keep numerical scoring constants here so that the ScoringService references
// a single source of truth rather than hard-coding magic numbers.
