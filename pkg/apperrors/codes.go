package apperrors

// TODO: define error code constants (e.g. CodeNotFound, CodeUnauthorised,
// CodeConflict, CodeInternal) and their mapping to HTTP status codes.
// These codes allow the handler layer to translate domain errors into
// appropriate HTTP responses without coupling the service layer to HTTP.
